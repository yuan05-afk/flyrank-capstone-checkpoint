/** Demo workspace limits for Checkpoint (Capstone / FlyRank demo). */
export const MAX_WIDGETS_PER_TENANT = 5;

/** Preferred widget count shown in a fresh demo workspace. */
export const DEMO_WIDGET_TARGET = 3;
